import React from 'react'

const ShowModal = ({ isOpen, onClose, object }) => {
    return (
        <div className="delete-modal__overlay">
            <div className="delete-modal__content">
                     
            </div>
        </div>
    )
}

export default ShowModal